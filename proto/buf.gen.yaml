version: v1
plugins:
- plugin: go
  out: .
  opt: paths=source_relative
- plugin: go-grpc
  out: .
  opt: paths=source_relative
managed:
  enabled: true
  go_package_prefix:
    default: github.com/strideynet/service-service/proto